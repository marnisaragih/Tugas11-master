<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.proteintracker">
    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ProteinTracker"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".List_Mahasiswa" />
        <activity android:name=".Edit" />
        <activity android:name=".Add" />
        <activity android:name=".KelolaMahasiswa" />
        <activity android:name=".FragmentAppMahasiswa" />
        <activity android:name=".Main3FragmentActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="" />

        <activity android:name=".HelpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>